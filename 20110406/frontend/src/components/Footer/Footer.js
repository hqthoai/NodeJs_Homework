import { Typography } from "@mui/material";

function Footer() {
    return ( 
        <div className="footer">
            <footer>Đây là footer</footer>
            <Typography variant="h6" component={"p"}>Vui lòng không coppy dưới mọi hình thức, mặc dù nó xấu :))</Typography>
        </div>
     );
}

export default Footer;